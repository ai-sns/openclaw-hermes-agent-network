// 获取所有具有 'imgcls' 类名的图片元素
var images = document.getElementsByClassName('imgcls');

// 遍历所有获取到的图片元素
for (var i = 0; i < images.length; i++) {
    // 将每个图片元素的显示样式设置为 'none'，以隐藏它们
    images[i].style.display = 'none';
}
as py 003