上线前准备
1.qwebbrowser优化
	a.\$\{
	b.html文件
	c.处理所有有问题的文档

2.跑任务模式的时候，如果problem或者content没有值会导致如下出错
def format_string_for_run_javascript(t_string):
    # \\`\\`\\`Python
    t_string = t_string.replace("\\\\`\\\\`\\\\`", "```")
    t_string = t_string.replace("\\`\\`\\`", "```")